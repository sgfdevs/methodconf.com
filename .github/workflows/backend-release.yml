name: Backend Release

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/backend-**'

permissions:
  contents: write
  issues: write
  pull-requests: write
  packages: write

jobs:
  release:
    runs-on: ubuntu-latest
    outputs:
      new-release-published: ${{ steps.semantic.outputs.new-release-published }}
      release-version: ${{ steps.semantic.outputs.release-version }}
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v6
        with:
          node-version: "lts/*"

      - name: Create Release
        id: semantic
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        working-directory: ./backend
        run: npx --package semantic-release@25 --package semantic-release-commit-filter --package conventional-changelog-conventionalcommits --package @semantic-release/exec semantic-release -e semantic-release-commit-filter

  docker:
    needs: release
    if: ${{ needs.release.outputs.new-release-published == 'true' }}
    uses: ./.github/workflows/backend-docker-publish.yml
    with:
      version: ${{ needs.release.outputs.release-version }}
    secrets: inherit
